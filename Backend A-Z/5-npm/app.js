console.log('hello');
console.log('안녕!');
/*
const _ = require('underscore');

// 💩
// forEach() 함수를 사용하면 되므로 더이상 underscore 라이브러리는 필요 없음
const array = [1, 2];
_.forEach(array, num => console.log(num));
*/
